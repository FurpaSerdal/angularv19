export interface etiket {
  etiketIsmi: string;   // Etiketin ismi
 etiketTipi: string;  // Etiketin tipi (örneğin: "rack_label", "a4_crossedout" vb.)
 ozelCss: string;     // Etiketin özel CSS dosyası (örneğin: "/assets/rack-label-style.css")
}  